name: CI/CD

on:
  pull_request:
  push:
    branches:
      - main
      - ci-cd-test

jobs:
  ci:
    name: Run tests
    runs-on: ubuntu-latest
    if: false
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: diplom
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d diplom"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Django tests
        env:
          DJANGO_SECRET_KEY: ci-secret-key
          DJANGO_DEBUG: "False"
          POSTGRES_DB: diplom
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_HOST: 127.0.0.1
          POSTGRES_PORT: "5432"
        run: python backend/manage.py test

  cd:
    name: Build, push and deploy
    runs-on: ubuntu-latest
    # needs: ci
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/ci-cd-test')
    env:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v3

      # - name: Login to Docker Hub
      #   if: env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != ''
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ env.DOCKERHUB_USERNAME }}
      #     password: ${{ env.DOCKERHUB_TOKEN }}

      # - name: Build and push
      #   if: env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != ''
      #   uses: docker/build-push-action@v6
      #   with:
      #     context: .
      #     push: true
      #     tags: ${{ env.DOCKERHUB_USERNAME }}/diplom:latest

      - name: Copy docker-compose file
        uses: easingthemes/ssh-deploy@v5.0.0
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          REMOTE_HOST: ${{ secrets.SERVER_HOST }}
          REMOTE_USER: ${{ secrets.SERVER_USER }}
          REMOTE_PORT: ${{ secrets.SERVER_PORT || 22 }}
          SOURCE: "docker-compose.prod.yml"
          TARGET: "/root/diplom_backend_prod/"
          ARGS: "-rlgoDzvc -i --delete"

      - name: Copy nginx config
        uses: easingthemes/ssh-deploy@v5.0.0
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          REMOTE_HOST: ${{ secrets.SERVER_HOST }}
          REMOTE_USER: ${{ secrets.SERVER_USER }}
          REMOTE_PORT: ${{ secrets.SERVER_PORT || 22 }}
          SOURCE: "nginx.conf"
          TARGET: "/root/diplom_backend_prod/"
          ARGS: "-rlgoDzvc -i --delete"

      # - name: Deploy via SSH (docker-compose v1)
      #   uses: appleboy/ssh-action@v1.0.3
      #   if: env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != ''
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     port: ${{ secrets.SERVER_PORT || 22 }}
      #     script: |
      #       set -e
      #       cd /root/diplom_backend_prod
      #       export DOCKERHUB_USERNAME="${{ secrets.DOCKERHUB_USERNAME }}"

      #       # Проверка, что docker-compose (v1) доступен
      #       docker-compose --version

      #       docker-compose -f docker-compose.prod.yml pull
      #       docker-compose -f docker-compose.prod.yml up -d --remove-orphans
      #       docker-compose -f docker-compose.prod.yml ps
